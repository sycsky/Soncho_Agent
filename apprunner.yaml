version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install --production=false
      - npm run build
run:
  command: npm start
  network:
    port: 8080
  env:
    - name: APP_PORT
      value: "8080"
